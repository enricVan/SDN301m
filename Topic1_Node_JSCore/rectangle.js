// m1
// exports.area = (l, w) => l * w
// exports.perimeter = (l, w) => 2 * (l + w)

// m2

const area = (l, w) => l * w
const perimeter = (l, w) => 2 * (l + w)

module.exports = {
  area,
  perimeter
}
